# End-of-Session Feedback: FleetCoordinator

## System-Level Feedback

- **Found a real, live cross-agent collision and fixed the README, not just the symptom.** Two independent Fleet Coordinators (this session and a concurrent "Antigravity"/`FleetCoordinator` one) were both operating on `newmexicoptg.org` through the shared primary clone rather than isolated worktrees. Mid-session, my own `git checkout main` landed a commit (`admin_migrate.php`'s scroll-lock fix) on `test-T-PTG-020` instead, because the other coordinator had switched the shared checkout to their branch between my commands. `bin/fleet.py`'s CLAIMED/IN_PROGRESS repo-lock only gates the `fleet claim` CLI call — it does nothing to protect raw git state in a shared working directory, so this was always going to happen sooner or later with more than one agent touching the same repo. Fixed by cherry-picking onto the right branch, then rewrote the README's "Checkout Drift" note from advisory ("if you know how, prefer worktrees") to a HARD REQUIREMENT with the actual mechanism explained, plus rewrote the Worker's "Do the Work" step to open with `git worktree add` instead of "create a `test` branch." This should be verified next session: did it actually stop the collisions, or does it need CLI-level enforcement (e.g. `fleet claim` refusing to proceed unless invoked from inside a worktree) rather than relying on every agent reading and following prose.
- **The README never said who merges to `main`/production or when — it had just been happening ad hoc.** Discovered when Chip explicitly told the other coordinator "workers merge to test... we shouldn't merge to main at this stage" and I realized `DONE` in the fleet's own lifecycle language ("The work is merged") doesn't distinguish an integration branch from production. Also discovered along the way: `origin/test` on `newmexicoptg.org` was stale by dozens of commits (everything since the color-schemes era) because nothing had actually been merging there — my own pushes today went straight to `main` under Chip's real-time direction, which was correct for those tasks but meant `test` silently rotted as a staging mirror. Fast-forwarded `test` to `main`'s tip (clean, no divergent content, verified via `git log origin/main..origin/test` / reverse being empty before the push) so it's a faithful starting point going forward, and added a "Merge Target — do not assume `main`" section to the README making this a standing, repo-checked policy (read the repo's own `deploy.yml`, don't assume) rather than a one-off instruction that only this session knows about.
- **Two recurring tooling gaps, both hit repeatedly by different Workers across both repos this session, still unfixed:** (1) `fleet verify`'s automated re-run keeps executing against the wrong checkout / hitting port conflicts rather than the Worker's actual worktree — every single Worker this session (T-INTY-021, T-INTY-018, T-PTG-008 resume, T-PTG-009 resume, T-PTG-010) had to hand-build a schema-valid `handoffs/*.yaml` instead of trusting the automated path. This is no longer a one-off fluke; it's the modal outcome. (2) Store-wide `fleet lint` fails on a single pre-existing broken file, `T-INTY-017.yaml` (`dod` vs `definition_of_done`, string vs array `scope`), that nobody has fixed all session despite at least four different agents (across today and, per its own history, prior days) noting it in passing and moving on. Worth either fixing it directly or archiving/CANCELLING it so it stops being permanent background noise in every lint run.
- **Schema gap, noted by a PM mid-session and worth escalating:** `schemas/task.schema.json` has no field for "PM made an explicit decision the Scout left open" — when a Scout deliberately defers 3-4 judgment calls to the PM (a good pattern, used well on `T-PTG-008` and `T-PTG-009`), the PM currently has nowhere schema-legal to record those decisions except folding prose into `scope` list items, which works but buries the decision trail inside general scope text rather than making it a distinct, queryable thing.

## Repository-Level Feedback

### `intypiano`

- Fixed a genuine, previously-undiagnosed local/CI regression (`T-INTY-021`): the 2026-08-11 multi-tenant demo-pool dispatch logic in `DatabaseManager.php` hardcoded a nonexistent `caut_sfusd` database as the fallback for the standard dev port (2027), silently breaking every `admin/v2/*` page and masking the true PHPUnit baseline (18 errors/114 failures, not the documented 259/0) for anyone working in this repo since that commit landed. This blocked `T-INTY-019`'s own testability for most of the session until fixed.
- Built the `gazelle_id` column work (`T-INTY-018`) carefully: the PM's initial audit assumed all 126 `piano_code` values were Gazelle IDs; the Worker's own investigation found only 42/126 actually were (the rest are hand-assigned QR codes like `ALUMNI`, `YAMAHA`), and scoped the backfill to a regex filter rather than a blanket copy — a real save, since a blanket copy would have fabricated 84 fake Gazelle identifiers. `piano_code` itself (physically printed on deployed QR labels) was proven untouched via before/after diff, not just asserted.
- `T-INTY-020` (Gazelle sync design doc) found the real API surface (`classes/integration/GazelleAPI.php`) only confirms `make`/`model` fields today — whether Gazelle exposes service history or tuning dates at all is unconfirmed, and the doc correctly refuses to design a sync for fields that might not exist rather than guessing.
- **Next steps for a human:** `T-INTY-019` ("Open in Gazelle" button) is `AUDITED` and unblocked (its `T-INTY-018` dependency closed), but genuinely cannot proceed without Chip confirming the real Gazelle web URL pattern — the codebase only has the private GraphQL API endpoint, not a browser-facing URL, and the PM correctly refused to let a Worker guess one.

### `newmexicoptg.org`

- Two production bugs were found and fixed the same day they shipped, both via real evidence rather than code inspection: `T-PTG-009` (the feature-request tag regex rejected `/featurerequest` with no separator — found by pulling real `debug_logs` rows 40 minutes after `T-PTG-008` shipped, a real member had already hit it) and the `admin_migrate.php` scroll-lock CSS bug (found live, while Chip was actively blocked trying to run a production migration, root-caused to the same shared-stylesheet `overflow:hidden` pattern that had already bitten `help.php` once before in this repo's own history — same bug class, never generalized to the other page that shared the stylesheet).
- `T-PTG-010` (contributor index) is the largest build of the session: a new normalized entity index (`contributors`/`article_contributors`) replacing a prompt-level hedge (`docs/answer-policy.md` §2.3, from `T-PTG-007`) with real, template-composed SQL answers — deliberately zero LLM-generated numbers so it structurally cannot hallucinate a count. The Worker found and fixed a real bug in my own implementation plan along the way (a duplicate `:answer` PDO placeholder in `recordDebugLog()` that would have silently broken under this repo's `EMULATE_PREPARES=false` setting, meaning every proof-of-fix log row would have failed to write) and discovered `CorpusIndexerTest.php` was dead code before this task touched it (PHPUnit-style class, no PHPUnit installed, no self-invocation — ran and produced zero output, exit 0, looking like a pass). The Reviewer independently reproduced both findings from scratch rather than trusting the Worker's narrative, including writing a standalone repro script for the placeholder bug against the real DB settings.
- **Concerns / next steps:** Chip's "conversation resolution write-back" idea (when a feature-request-originated task ships, post a system message back into the original member's conversation thread saying so) was discussed and design-sketched in conversation but deliberately not scoped as a task yet — it depends on the not-yet-built extraction script that turns `feature_request_conversations` rows into fleet tasks in the first place, which `T-PTG-008`'s own DoD explicitly stopped short of building. Both are real, wanted follow-on work, not forgotten.
- `T-PTG-016`'s security fix (admin_reply.php IDOR) is merged and live in production, but Chip still needs to personally run `promote_admin.php <his-email>` against the production database before he can use `admin_reply.php` again — the Worker could not run this himself (no production DB credentials reachable from this environment) and the task's own DoD required handing Chip the exact command rather than leaving this undiscoverable.
- A second, independent swarm (`Antigravity-Worker` / a concurrent `FleetCoordinator`) was actively building JournalGPT v3 (Phases 0 through 1b done, Phase 2 in flight, Phases 2b-6 pre-audited and queued) throughout this session. I deliberately did not compete for that repo's claim lock — added value instead by clearing the review backlog and fixing coordinator-level process gaps (worktrees, merge-target policy) that benefit both swarms rather than duplicating build work already well in hand.
