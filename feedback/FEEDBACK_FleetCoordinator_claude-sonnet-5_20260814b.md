## System-Level Feedback

- **Most of today's session ran outside the fleet's claim/audit/submit lifecycle entirely**, and I think that was the right call, not a shortcut — worth the coordinator's designers knowing why. After T-INTY-025 closed, the user drove an interactive, fast-moving investigation (a production bug report → root-cause it → fix it → verify it → the next question building directly on what was just found, repeated ~15 times over the session) where the overhead of writing a task YAML, auditing it against a pinned repo-sha, and waiting for claim before touching code would have actively gotten in the way of "the user just asked a follow-up based on what I found 30 seconds ago." The fleet's task model assumes work is plannable in advance; a debugging/build session driven by live findings is not that shape. Every change was still committed individually with a real commit message, tested (`phpunit` baseline compared before/after every single commit, not just at the end), and deployed with verification — the rigor stayed, just not inside a YAML task record. Recommend the README explicitly bless this mode for a solo interactive session rather than implying every change needs a task, so the next agent doesn't waste time forcing an interactive debugging session into task shape.
- **`fleet verify`'s worktree gap (flagged in my 08-14 feedback file) never got exercised again today** because nothing after T-INTY-025 went through `claim`/`submit` — can't add new evidence on it, but it's still unfixed as far as I know.
- **Same collision-risk pattern from earlier in the day recurred**: mid-session, another agent's uncommitted work (a versioned-changelog feature, later confirmed as commit `6882aecf`) was sitting in the primary `intypiano` clone alongside my own investigation. I left it alone and it resolved itself before I needed to touch that area, but this is now the second time in one day a solo interactive session in the primary clone nearly stepped on concurrent work. If solo/interactive mode (see above) becomes an explicitly blessed pattern, worth pairing it with a stronger convention than "check `git status` and hope" — e.g., a lightweight advisory lock file in the primary clone that other agents check before starting unstructured work there.

## Repository-Level Feedback (intypiano)

Long session, roughly 15 distinct pieces of work, all committed and deployed to production individually (see `docs/superpowers/2026-08-14-session-handoff.md` in the intypiano repo for the full list with commit-level detail — not duplicating it here). Highlights and what I'd flag for a human or the next agent:

**The `password_resets.tuner_id` bug was the session's real find.** Password reset had been silently minting zero tokens on every affected tenant since `ddl/140` shipped, invisible because the failure was caught and swallowed by design (graceful degradation for un-migrated databases, which happened to also mask a genuine schema bug). Fixing it the naive way (a plain `.sql` `CHANGE COLUMN`) would have been actively harmful — it broke 6 of 7 tenants on the first attempt because they'd already gotten the correct column via some path outside the tracked migration history. The fix that actually worked required making the migration check `information_schema` first. This is the second time this session-family has found "the tracked migration state doesn't match reality" (first was `ddl_applied`/`version` drift a session or two back per the doc history) — I'd treat that as a real pattern in this codebase, not a one-off, and lean toward idempotent/self-checking migrations as the default going forward rather than the exception.

**Built real diagnostic capability, not just fixes.** `compare_workload_factors.php` in particular is worth calling out: rather than reimplementing 1400 lines of DOM-coupled JS to answer "does v1 and v2 produce the same workload numbers," it diffs the six actual input fields per piano and proves the answer by construction. That pattern (verify inputs match rather than re-derive and hope the port is faithful) is reusable for other v1-vs-v2 parity questions this project will keep having as long as both schemas coexist.

**Every single change was verified live, not asserted.** Multiple UI fixes this session used the `browse` skill against a real running server with a real authenticated session — not `php -l` and a guess. Caught at least one thing that would have shipped wrong otherwise: the button-disable fix worked functionally on the first attempt but the expected `opacity: .6` visual styling silently didn't apply, because the CSS rule I assumed was global (`theme.css`) turned out not to be loaded on that specific page. Would not have caught that without actually screenshotting/inspecting the live DOM.

**Next steps for the human**, all stated explicitly to the user during the session, repeating here for continuity: add the `INTYPIANO_API_TOKEN` GitHub Actions secret (deploy-time verification is built and deployed but inert without it); confirm `forgot_password.php` emails are landing in an actual inbox, not just accepted by the local MTA; decide whether to commit or discard the two untracked real-data files sitting in `databasedumps/`; `bookingTrace.php`/`assignToPerson.php` still hardcode `request_unified` with no fallback (explicitly deferred by the user, not forgotten).
