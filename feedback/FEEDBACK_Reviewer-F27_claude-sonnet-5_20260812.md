# Feedback: Reviewer-F27 (claude-sonnet-5), 2026-08-12

## System-Level Feedback

- The review-YAML template generated by `start-review` produces multi-line
  `description` fields as default YAML strings, and a plain (unquoted) scalar
  breaks the moment the text contains a bare `key: value`-shaped substring
  (e.g. quoting a `grep` command or describing a Python dict literal). Hit
  `yaml.scanner.ScannerError: mapping values are not allowed here` on first
  `record-review` attempt because a description had `... eyeballed): \`...\``
  in it. Recommend either (a) `start-review` pre-seed `description:` fields
  with the `>-` block-scalar indicator so agents fall into a safe pattern by
  default, or (b) `record-review` give a clearer error pointing at the
  offending YAML line/field instead of a raw traceback. Cost me one extra
  round trip; trivial to avoid.
- Everything else about the review flow (`start-review` -> fill YAML ->
  `record-review`) worked exactly as documented and moved the task straight
  to `DONE` in one call, with no separate close step needed for a small
  key-order fix like this.

## Repository-Level Feedback

Reviewed T-MIN-020, a narrow key-order-only fix in
`research/05-registry-and-audit/Stage5_Master_Card_Registry.json`: moving the
`aliases` key of the `SPECIAL-FOOL` card object to sit immediately after
`historical_names`, matching the precedent T-MIN-017 set for the four
cavalier rows. Did the review in an isolated `git worktree` checked out at
the audited head sha (8a2a7e73) so the shared `minchiate_tarot` checkout
never left the `test` branch it was found on.

Verified programmatically, not by eyeballing the diff: loaded both the base
(19d0e60) and head (8a2a7e73) versions of the full JSON via `git show
<sha>:path` into Python dicts, built `card_id`-keyed maps of all 97 cards on
each side, and diffed them. Every single card's dict representation
(order-insensitive) is identical between base and head — including
SPECIAL-FOOL itself, whose `aliases` value is still exactly `["TRUMP-FOOL"]`.
A separate per-card key-order comparison found exactly one card with any
order difference: SPECIAL-FOOL, confirming the change really is
key-order-only, touches nothing else in the file, and doesn't disturb the
cavalier rows T-MIN-017 already fixed. `git diff --name-only` between base
and head confirms exactly one file changed. Re-ran the audited
`verification_command` in the worktree; both its python3 assertions and its
`grep -A20`/`grep -q aliases` check passed.

This is a clean, well-scoped worker submission with no issues — verdict
PASS, task now DONE. Worth noting for future scouts: T-MIN-017's own review
already flagged this exact SPECIAL-FOOL gap as a MINOR follow-up item before
T-MIN-020 existed to fix it, so the scout -> PM -> worker -> reviewer pipeline
correctly closed a loop that a previous review had explicitly deferred.
No further action needed on this specific registry file; the `grep -A20`
windowing fragility class (this task's second occurrence) may still exist
elsewhere in the codebase for other multi-field JSON objects — a scout could
usefully do one broader sweep to confirm there are no remaining occurrences
rather than fixing them one card at a time.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
